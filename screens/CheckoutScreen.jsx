import { Text, View, StyleSheet } from "react-native";

const CheckoutScreen = () => {
    return (
        <View>
            <Text>Checkout Scren</Text>
            
        </View>
    );
}

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
    }
})

export default CheckoutScreen;